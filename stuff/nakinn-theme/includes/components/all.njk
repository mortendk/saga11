{%- if env.mode != 'prod' %}<!-- src/__includes/component/all.liquid -->{%- endif -%}
{#
  Tales from the loop ...
  ---------------------------------------------------------
  This is where the magic happens and we loop through
  all the componenets and render them out as specified from the template files
  and the componenet type
#}
{%- if env.mode != 'prod' %}<!-- src/__includes/component/all.liquid -->{%- endif -%}

{% comment %}use async so images work {% endcomment %}
{% asyncEach component in components %}

  {% include component.template %}

{% endeach %}
