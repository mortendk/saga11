{%- if env.mode != 'prod' %} <!-- src/theme-grunn/includes/components/form/field/input.njk -->{%- endif  -%}
<div>

  <label for="form-item-{{ item.title | slugify }}">
    {{ item.title }}
    {% if item.required %} * {% endif %}
  </label>

  <input type="{{ item.inputtype }}" name="{{ item.title | slugify }}" id="form-item-{{ item.title | slugify }}" placeholder="{{ item.placeholder }} " {% if item.required %}required{% endif %} />
</div>
