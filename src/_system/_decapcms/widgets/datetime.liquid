      - label: "🕒 {{ label }}"
        name: "{{ name }}"
        widget: "datetime"
        i18n: true
        date_format: "DD.MM.YYYY"
        time_format: "HH:mm"
        format: "YYYY-MM-DD HH:mm:ss"
        picker_utc: true
