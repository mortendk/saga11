      - label: "{{ label }}"
        name: "{{ name  }}"
        widget: "number"
        {% if default %}default: {{ default }}{% endif %}
        {% if required %}required: {{ required }}{% endif %}
        {% if hint %}hint: "{{ hint }}"{% endif %}
        value_type: {% if value_type %}{{ value_type }}{% else %}int{% endif %}
        min: {{ min}}
        max: {{ max }}
        step: {{ step }}
