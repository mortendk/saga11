      - label: "📷 {{ label }}"
        name: "{{ name }}"
        widget: "image"
        {% if default %}default: {{ default }}{% endif %}
        choose_url: {% if choose_url %}{{ choose_url }}{% else  %}false{% endif %}
        {% if required %}required: {{ required }}{% endif %}
        {% if hint %}hint: {{ hint }}{% endif %}
        media_library:
          config:
            multiple: true
            media_folder: "src/content/upload/"
