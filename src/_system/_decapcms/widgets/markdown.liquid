      - label: "👩‍💻 {{ label }}"
        name: "{{ name }}"
        widget: "markdown"
        buttons:
          - link
          - bold
          - italic
          - heading-one
          - heading-two
          - heading-three
          - code
          - quote
          - bulleted-list
        editor_components:
          - shortcode-picture
          - code-block
        {% if required %}required: {{ required }}{% endif %}
        {% if hint %}hint: {{ hint }}{% endif %}
        {% if default %}default: {{ default }}{% endif %}
