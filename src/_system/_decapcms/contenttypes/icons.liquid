  - name: icons
    label: "icons"
    folder: "src/{{ theme }}/includes/icons"
    slug: "{{title}}"
    hide: true
    format: frontmatter
    extension: svg
    fields:
      - { label: "name", name: "name", widget: "string" }

