  - name: icons
    label: "icons"
    folder: "src/themes/{{ theme }}/includes/icons"
    slug: "{{title}}"
    hide: true
    format: frontmatter
    extension: svg
    fields:
      - { label: "name", name: "name", widget: "string" }

