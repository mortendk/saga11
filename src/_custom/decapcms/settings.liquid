{% comment %}
  The settings can be used to edit json files ex for pernalink for a contenttyle
  Its inserted to a yaml file so the spacing is there for a reason
{% endcomment %}

{% comment %}


      - label: "💜 foobar"
        name: "foobar"
        hide: true
        file: "src/content/foobar/foobar.json"
        fields:
          - { label: text, name: text, widget: string  }

{% endcomment %}
