---
layout: default.liquid
saga-template-name: Frontpage
---
{%- if env.mode != 'prod' %}
<!-- 👋 sections src/theme-grunn/layouts/page/frontpage.liquid -->
{%- endif -%}

{%
  # this is a comment
%}


{% assign all = collections.saga11  | sort_natural: "data.title"  %}
{% for item in all  limit: 10 %}

  {{ item.data.order }} -
  {{ item.data.title }}
<br>

{% endfor %}

{% comment %}


<article class="flex flex-col md:flex-row">

  {% render
      'image.liquid',
        file: image,
        alt: title,
        sizes: "(min-width: 30em) 50vw, 100vw",
        widths: "400, 800, 1200",
        format: "webp",
        loading: "lazy",
        css: "w-full"
 %}

  <div class="prose prose-xl dark:prose-invert mt-4 md:mt-0 md:ml-4 p-4 bg-white rounded-md shadow-md dark:bg-gray-700">
    {{ content }}
  </div>
</article>

{% include "components/all.liquid" %}
{% endcomment %}
