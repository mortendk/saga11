---
layout: default.njk
saga-template-name: Frontpage
---
{#
------------------------------------------------
FRONTPAGE TEMPLATE
------------------------------------------------
Specfic page to control the layout for the frontpage
#}
{%- if env.mode != 'prod' %}<!-- ___layouts/page/frontpage.njk  -->{%- endif -%}


  {# {% picture
      img=card.image,
      alt=card.alt,
      width=[400,800, 1600],
      sizes="(min-width: 1600px) 50vw, 100vw",
      css="shadow rounded-lg h-96 overflow-hidden",
      loading = "eager"
  %}
 #}


{# {% include "src/_system/debug/global.njk" %} #}
{# {{ globalThis | dump | safe }} #}

{% for item in collections.allPages | sortByOrder %}

  {% if loop.first %}
    {# {{ item | debugpretty("item") | safe  }} #}
  {% endif %}

{% endfor %}

<section class="relative mb-4">
  {% picture
      img=card.image,
      alt=card.alt,
      width=[400,800, 1600],
      sizes="(min-width: 1600px) 50vw, 100vw",
      css="shadow rounded h-96 overflow-hidden",
      loading = "eager"
  %}
  <article class="absolute bg-white/80 w-1/2 top-0 p-8 h-96">
    <h1 class="text-7xl text-cyan-700 ">{{ title }}</h1>
    <div class="my-4 text-2xl">{{ card.teaser | safe}}</div>
  </article>

</section>

{# {% include "component/pagecard.njk" %} #}

{% include "components/all.njk" %}
