{# image
------------------------------------------------
#}
{%- if env.mode != 'prod' %}<!-- src/__includes/sections/image.njk  -->{%- endif -%}

{% if section.image %}
    {{ section.image    }}
    {{ section.title    }}
  <div class="wysiwyg">
    {{ section.text  | markdown | safe  }}
  </div>
{% endif %}

