{# COLLECTION OF POSTS
------------------------------------------------
Creates a list of a collection with a chosen tag from the
markdowm
collection: tagname

https://www.11ty.dev/docs/collections/
#}
{%- if env.mode != 'prod' %}<!-- __includes/section-collection.njk  -->{%- endif -%}


{% set collection = section.collection  %}
{% if collection %}

<h3 class="text-3xl">{{ section.title }} </h3>
<div class="wysiwyg">{{ section.text | markdown | safe  }}</div>

<div class="grid md:grid-cols-3 gap-4 my-4" aria-label="Collection">


  {% for item in collections[section.collection] | sortByOrder %}
  {%- if env.mode != 'prod' %}<!-- markdown file: {{ item.inputPath }}  -->{%- endif -%}

  <article class="p-4 shadow border rounded" aria-label="Post {{ item.data.title}}" >

    <a href="{{ item.url }}" title="{{item.data.title}}" class="block text-xl font-bold sr-tab" tabindex="0">{{item.data.title}}
      <div class= "aspect-video overflow-hidden">

        {% image img=item.data.card.image, width=[300, 600] %}

      </div>
    </a>

    <div class="mt-2">
      {{ item.data.teaser }}
    </div>
  </article>

{% endfor %}

</div>

{% endif %}

